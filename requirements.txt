# PR Review bot — runtime dependencies.
# code-review-graph is already installed in the .venv and provides the
# deterministic layer; it is intentionally not re-pinned here.
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
openai>=1.40
PyJWT>=2.8
cryptography>=42
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# dev / tests
pytest>=8.0
pytest-asyncio>=0.23
respx>=0.21
